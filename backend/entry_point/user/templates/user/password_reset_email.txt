Точка входа — Сброс пароля

Здравствуйте{{ user.name|default:"" and (" " + user.name) or "" }}!

Вы запросили сброс пароля. Ваш токен:

{{ token }}

Токен действителен {{ timeout }} минут.

Чтобы сменить пароль, отправьте POST /auth/password-reset/confirm/ с полями `token` и `new_password`.

Если вы не запрашивали сброс — проигнорируйте это письмо.

Письмо отправлено приложением "Точка входа". Обратная связь: {{ from_email }}
